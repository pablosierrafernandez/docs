---
title: "List Evaluators"
description: "Returns all evaluators for the authenticated account, sorted by creation date (newest first)."
openapi: 'api-reference/openapi-evaluators.json POST /list-evaluators'
---

<Note>
  Results are not paginated. `data.count` reflects the total number returned (max 20 per account).
</Note>
